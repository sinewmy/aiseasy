# aiseasy

This repository is organized to support multiple learning projects.

- `agent_basic/` contains the current simple AI agent project.

Each project folder is self-contained with its own code, notebook, and dependencies.

## Current project

See `agent_basic/README.md` for details on the simple AI agent project.
